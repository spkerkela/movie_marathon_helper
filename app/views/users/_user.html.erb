<li>
<%= link_to user.name, user, class: "btn btn-inverse btn-block" %>
<% if current_user.admin? && current_user != user %>
	<%= link_to "delete", user, class: "btn btn-danger pull-left", method: :delete, data: { confirm: "Are you sure you want to delete #{ user.name}?"} %>
<% end %>
</li>